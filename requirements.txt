# Core (pinned for compatibility with torch 1.13.1 + cu117)
numpy==1.23.5
pyyaml>=6.0
pycocotools>=2.0.2
omegaconf>=2.0
timm==1.0.20

# MMDet/YOLO ecosystem
# Install mmengine/mmcv/mmdet via openmim; keep mmyolo here.
mmyolo==0.6.0

# Data processing & utils (pinned to avoid numpy>=2 pressure)
tqdm>=4.65.0
opencv-python<4.11
pillow>=9.5.0
matplotlib<3.9
pandas<2.2
scikit-learn>=1.0.0
scikit-image<0.24
scipy<1.13
albumentations==1.3.1
ensemble-boxes
wandb
